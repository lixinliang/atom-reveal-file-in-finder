'use babel';

import AtomRevealFileInFinderView from '../lib/atom-reveal-file-in-finder-view';

describe('AtomRevealFileInFinderView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
